# deploy_namespace.yaml - Creates the keycloak-app and ingress namespace in the cluster
- name: create namespace for keycloak
  hosts: localhost
  tasks:
    - name: Apply Keycloak namespace
      kubernetes.core.k8s:
        kubeconfig: ~/.kube/config
        state: present
        src: ../../kube/namespace.yaml