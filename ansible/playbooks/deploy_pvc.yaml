- name: Deploy Persistent Volume Claim
  hosts: localhost
  tasks:
    - name: Apply PVC deployment
      kubernetes.core.k8s:
        kubeconfig: ~/.kube/config
        state: present
        src: ../../kube/postgres-pvc.yaml
        namespace: keycloak-app 